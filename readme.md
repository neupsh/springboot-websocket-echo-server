

## How to run
Using docker-compose:
```sh
docker-compose up
```
If you want to run after changes in the source
```sh
docker-compose up --build
```

## How to build
Using gradle:
```sh
./gradlew bootJar
```
